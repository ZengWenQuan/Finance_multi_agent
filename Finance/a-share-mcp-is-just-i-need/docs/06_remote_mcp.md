# 远程 MCP 部署

本地 MCP 使用 `stdio`。远程 MCP 使用网络传输，但工具 schema 和行为保持一致。

关键差异：
- 服务端长期运行
- 客户端通过 HTTP 或 websocket 连接
- 需要鉴权、超时和限流

推荐做法：
1. 工具函数尽量保持纯粹和可复现。
2. 用一层薄服务暴露相同工具名。
3. 鉴权放在传输层处理，不侵入工具逻辑。
4. 监控延迟，对重查询做缓存。

适合远程化的场景：
- 多客户端共享同一数据源
- 需要统一管理凭据或日志
- 希望独立扩容工具执行
